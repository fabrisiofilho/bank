import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "light",
    text: {
      primary: '#1e2023',
    },
    primary: {
      main: '#1da584',
      contrastText: '#fff',
    },
    secondary: {
      main: '#fff',
    },
    background: {
    }
  },
});
